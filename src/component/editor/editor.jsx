import React from 'react';
import styles from './editor.module.css';

const Editor = props => (
    <setion className={styles.editor}>
        <h1 className={styles.title}>Card Maker</h1>
    </setion>
);

export default Editor;